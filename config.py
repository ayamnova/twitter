# The path to the root folder where the Flume files are stored
PATH = "../REU/data/"
# The path to the root folder where processed data is stored
PROC = "../REU/proc/"
# The path to the root folder where outputted data (graphs, images, etc) is
# stored
OUT = "../REU/out/"

# Filter options

# Only tweets in English
LANG = ["en"]
